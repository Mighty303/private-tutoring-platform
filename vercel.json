{
  "buildCommand": "cd tutor-website && npm install && npm run build",
  "outputDirectory": "tutor-website/dist",
  "installCommand": "echo 'skip'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
